
#include <stdio.h>
int main(void)
{
int h=10;
int w=20;
int a=0;
int p=0;

printf("A rectangle, which height is %d cm and width is %d cm \n", h, w );
a = h*w;
p =(h*2)+(w*2);
printf("the perimeter is equal to %d cm \n", p );
printf("the area is equal to %d square cm \n", a );
}
